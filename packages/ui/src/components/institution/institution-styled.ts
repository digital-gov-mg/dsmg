import { styled } from '../../config'

export const InstitutionStyled = styled('div',{
    maxWidth: '320px',
    fontWeight: 'bold',
})

export const CountryRegimeStyled = styled('div', {
    fontWeight: 'lighter',
    marginTop: '6px'
})

export const CountryMottoStyled = styled('div', {
    fontWeight: 'lighter',
    fontStyle: 'italic',
    marginTop: 0
})
